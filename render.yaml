services:
  - type: web
    name: my-portfolio
    env: static
    buildCommand: npm run build
    staticPublishPath: dist  # <-- THIS LINE IS NOW CORRECT FOR VITE
    routes:
      - route: /(.*) 
        handler: spa
    # ----------------------------------------------------